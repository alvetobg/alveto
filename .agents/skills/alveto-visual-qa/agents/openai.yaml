interface:
  display_name: "Alveto Visual QA"
  short_description: "Judge real screenshots across all target viewports"
  default_prompt: "Use $alveto-visual-qa to inspect real browser screenshots and issue a visual acceptance verdict for Alveto."
policy:
  allow_implicit_invocation: true
